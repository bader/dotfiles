- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/nvim: neovim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [powershell -ExecutionPolicy Bypass -File scripts/install-neovim.ps1, Installing Neovim]
